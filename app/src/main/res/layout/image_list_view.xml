<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="2dp">

    <ImageView
        android:id="@+id/imageView_images_list"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:scaleType="centerCrop"
        tools:srcCompat="@tools:sample/backgrounds/scenic[1]" />

    <ImageView
        android:id="@+id/check_box_images_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignStart="@id/imageView_images_list"
        android:layout_alignTop="@id/imageView_images_list"
        android:layout_alignEnd="@id/imageView_images_list"
        android:layout_alignBottom="@id/imageView_images_list"
        android:padding="45dp"
        android:visibility="gone"
        app:srcCompat="@drawable/ic_check" />

</RelativeLayout>